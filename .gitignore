# Build files
.gradle
.kotlin
build

# IDE files
*.iml
.idea/*
!.idea/icon.svg
/captures

# Configuration files
local.properties

# Local secrets — copy .env.example to .env and fill in fork-specific values
.env
.env.local
*.keystore
*.jks

# Firebase project credentials — fork users supply their own google-services.json
# (must NOT be committed; Firebase API key + project IDs leak otherwise)
app/google-services.json
.google-services-base64.txt

# macOS specific files
.DS_Store
